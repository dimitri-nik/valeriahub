/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['supercell.com', 'drive.google.com'], // Add any other allowed domains here
  },
};